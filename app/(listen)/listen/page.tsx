import { Suspense } from "react";

export default function ListenMainPage() {
  return (
    <div className="text-center h-96 flex justify-center items-center">
      {/* <Suspense fallback={<p className="text-red-600">Server Error</p>}>
        <audio src="https://stream.radiojar.com/8s5u5tpdtwzuv"></audio>
      </Suspense> */}
    </div>
  );
  // TODO: it should be painted in black bro;
}
